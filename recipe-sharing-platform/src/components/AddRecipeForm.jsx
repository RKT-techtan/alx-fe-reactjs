import { Route, Link, useRouteMatch, useEffect } from 'react-router-dom';
import { components, useState } from 'react';

function AddRecipeForm () {
	const handleSubmit = () => {steps};
	const [target.value] = useState(0);
	<form onSubmit={handeSubmit}>
		document.title= 'Success';
	return (
	<div>
		<h1>ingredients</h1>
		<form>
		</form>
		<button style="onSubmitt">Click</button>
	</div>
	);
};
